use std::rc::Rc;

use parser::token::Token;

// use object::object::PyObject;

#[derive(Debug)]
pub struct Node {
    leaf: Token,
    // child: Vec<Rc<PyObject>>,
    // left: Option<Box<PyObject>>,
    // right: Box<PyObject>,
}

impl Node {

    pub fn eval() {

    }
}